apply plugin: 'com.android.library'

version = "1.4.2"

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.1"

    defaultConfig {
        minSdkVersion 1
        targetSdkVersion 23
        versionCode 8
        versionName version
    }

}
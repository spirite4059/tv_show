<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vego.player"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="3"
        android:targetSdkVersion="19" />

    <!-- =============================================================== -->
    <!-- The OpenYouTubeActivity requires these permissions -->
    <!-- =============================================================== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application android:label="@string/app_name" >
        <activity
            android:name="com.vego.player.MainActivity1"
            android:label="@string/app_name" >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <!-- =============================================================== -->
        <!-- This is what you'll need to include in your manifest to declare -->
        <!-- the OpenYouTubeActivity in your project -->
        <!-- =============================================================== -->
        <!-- <activity -->
        <!-- android:name="com.vego.player.vego.player.OpenYouTubePlayerActivity" -->
        <!-- android:configChanges="orientation" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:screenOrientation="landscape" -->
        <!-- android:theme="@android:style/Theme.Black" > -->
        <!-- </activity> -->
    </application>

</manifest>